export default {
  getParameterString(astType) {
    return astType._text;
  },

  getReturnString(astType) {
    return astType._text;
  },

  getDescriptionString(astType) {
    return astType._text;
  },
};
